environment: dev

queueName: my_first_sns_queue

logging:
  level: INFO
  appenders:
    - type: console
      threshold: ALL
      timeZone: UTC
      target: stdout
      logFormat: "[%p] %date{dd MMM yyyy;HH:mm:ss.SSS} | %msg%n"